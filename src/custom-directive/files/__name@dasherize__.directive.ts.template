import { Directive, ElementRef, Renderer2 } from '@angular/core';

@Directive({
  selector:"my-<%= dasherize(name) %>"
})
class My<%= classify(name) %>Directive { 
  constructor(private el: ElementRef, private renderer2: Renderer2) {}
}
